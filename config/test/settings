databaseUrl = "host=$(TEST_DB_HOST) dbname=$(TEST_DB_NAME) user=$(TEST_DB_USER) password=$(TEST_DB_PASS) port=$(TEST_DB_PORT)"
poolSize = 1
runMigrations = true
httpPort = "$(TEST_HTTP_PORT)"
verboseLogging = false
